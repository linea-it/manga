import { makeStyles } from '@material-ui/core/styles';

const styles = makeStyles({
  root: {
    textAlign: 'center',
  },
  imgContainer: {
    margin: '0 5%',
  },
  img: {
    width: 'auto',
    height: 'auto',
    maxWidth: '100%',
  },
});

export default styles;
